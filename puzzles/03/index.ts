import { sumValues } from "../common/math";

const a = (lines: string[]) => lines.map(findIntersection).reduce(sumValues, 0);
const b = () => void 0;
const findIntersection = (line: string): number => {
  return 0;
};
export { a, b };
